# Endereço do servidor. Como está rodando localmente, usamos 'localhost'.
DB_SERVER=localhost

# Nome do banco de dados. O SQL Server tem um banco padrão chamado 'master'.
# Para começar, podemos usá-lo. Mais tarde, você pode criar seu próprio banco.
DB_DATABASE=master

# O usuário administrador padrão do SQL Server é 'sa' (System Administrator).
DB_USERNAME=sa

# A senha que você DEFINIU no comando docker run.
DB_PASSWORD=SuaSenhaSuperForte#123

# O nome do driver ODBC. Veja o Passo 4 para verificar o seu.
DB_DRIVER={ODBC Driver 17 for SQL Server}